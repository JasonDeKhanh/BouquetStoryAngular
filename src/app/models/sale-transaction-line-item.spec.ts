import { SaleTransactionLineItem } from './sale-transaction-line-item';

describe('SaleTransactionLineItem', () => {
  it('should create an instance', () => {
    expect(new SaleTransactionLineItem()).toBeTruthy();
  });
});
